<h1>TEAM5OAGS Chat</h1>

<ul id="chat">
  <%= render @messages %>
</ul>

<%= form_for Message.new, :remote => true do |f| %>
    <br>
    <%= f.text_field :content %>
    <%= f.submit "Send" %>
    <br>
<% end %>